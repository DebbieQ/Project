- name: "Create backend app directory"
  file:
    path: homw/ubuntu/backend-app
    state: directory

- name: "copy files"
  become: true
  copy:
    src: /root/project/artifact.tar.gz
    dest: /home/ubuntu/backend-app

- name: "Unarchive"
  become: true
  shell: |
    cd /home/ubuntu/backend-app
    tar xvzf artifact.tar.gz -C .

- name: "run app"
  shell: |
    cd home/ubuntu/backend-app  
    npm install
    pm2 stop default
    pm2 start npm -- start
